# Secrets and local env
.env
*.pem

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# OS / editor
.DS_Store
.idea/
.vscode/

# Cron / local logs
logs/

# Local data (DB, journals, ledger — regenerate or keep private)
data/*.db
data/*.db.bak*
data/*.db.*.bak
data/*.jsonl
data/execution_ledger.json
data/statcast_cache/

# Trained binaries and calibration artifacts (retrain with `python run_pipeline.py train`)
models/*.pkl
models/*.json

# Market snapshots (regenerate with `snapshot` command)
data/snapshots/
